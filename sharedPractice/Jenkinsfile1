library identifier: 'jenkinsSharedLibrary@main',
retriever: modernSCM ([$class: 'GitSCMSource', remote: 'https://github.com/gourPunit/jenkinsSharedLibrary.git'])

pipeline {
    agent {
        docker {
            image 'maven:latest'
            args '-u root'
        }
    }
    stages {
        stage('Audit Tools') {
            steps {
                auditTools()
            }
        }
    }
}